plugin: aws_ec2
regions:
  - eu-central-1
filters:
  tag:type:
    - web 
    - db 
compose:
  ansible_host: private_ip_address
keyed_groups:
  - prefix: env
    key: tags['env']
  - prefix: type
    key: tags['type']